@model SharpBlog.Models.Post

@{
    ViewBag.Title = "@Model.Title";
}

<div class="post">
    <h2 class="post__title">@Model.Title</h2>
    <div class="post__body">@Html.FromMarkdown(Model.Body)</div>
</div>